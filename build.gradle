plugins {
    id 'grails-website'
}

// All properties now have sensible defaults via conventions.
// Only configure if you need to override them:
//
// grailsWebsite {
//     url = 'https://custom-domain.com'
//     title = 'Custom Title'
// }
